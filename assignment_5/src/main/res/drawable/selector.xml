<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:exitFadeDuration="@android:integer/config_mediumAnimTime" >

    <!-- I never see this one - the grid items are not focusable -->
    <item android:state_pressed="false" android:state_focused="true" android:drawable="@color/black" />

    <!-- while pressed -->
    <item android:state_pressed="true" android:drawable="@color/black" />

    <item android:state_selected="true" android:drawable="@color/black" />

    <!-- while selected in actionmode -->
    <item android:state_activated="true" android:drawable="@color/black" />

    <item android:drawable="@android:color/transparent" />
</selector>